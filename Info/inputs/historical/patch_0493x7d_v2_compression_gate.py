#!/usr/bin/env python3
from pathlib import Path
import subprocess

ROOT = Path(__file__).resolve().parents[1]
EXPECTED_HEAD = "c47f49f8af4cd24e3c551754b2297a37fa173d50"

def run(*args):
    return subprocess.run(args, cwd=ROOT, text=True, stdout=subprocess.PIPE, stderr=subprocess.STDOUT, check=False)

def replace_once(rel, old, new, label):
    path = ROOT / rel
    text = path.read_text()
    n = text.count(old)
    if n != 1:
        raise SystemExit(f"[0493x7d-v2] ERROR {label}: expected 1 match in {rel}, got {n}")
    path.write_text(text.replace(old, new, 1))

head = run('git','rev-parse','HEAD').stdout.strip()
branch = run('git','branch','--show-current').stdout.strip()
if branch != 'surf':
    raise SystemExit(f"[0493x7d-v2] ERROR expected branch surf, got {branch!r}")
if head != EXPECTED_HEAD:
    raise SystemExit(f"[0493x7d-v2] ERROR expected HEAD {EXPECTED_HEAD}, got {head}")
tracked = run('git','status','--short','--untracked-files=no').stdout.strip()
if tracked:
    raise SystemExit("[0493x7d-v2] ERROR tracked working tree not clean:\n" + tracked)

replace_once('include/simulation_params.h', '    double q6DensityRelaxationBeta = 0.0;\n    double q6DensityRelaxationTime = 0.0;\n\n    bool projectionImmersedSolidMaskEnable = false;\n', '    double q6DensityRelaxationBeta = 0.0;\n    double q6DensityRelaxationTime = 0.0;\n\n    // 0493x7d-v2 experimental compression/noise discriminator. Disabled by\n    // default: false preserves the historical rawFill x7d target bit-for-bit.\n    // When enabled, positive density relaxation is admitted only when the\n    // center defect and at least one direct face-neighbour exceed the same\n    // fill threshold. The Q6-G-F runner writes thetaN/GAMMA here.\n    bool q6DensityRelaxationCompressionGateEnable = false;\n    double q6DensityRelaxationCompressionThresholdFill = 0.0;\n\n    bool projectionImmersedSolidMaskEnable = false;\n', 'SimulationParams x7d-v2 fields')
replace_once('src/params_io_base.cpp', '        else if (key == "q6DensityRelaxationBeta" || key == "densityRelaxationBeta") p.q6DensityRelaxationBeta = parse_double(value, key);\n        else if (key == "q6DensityRelaxationTime" || key == "densityRelaxationTime" || key == "densityRelaxationTau") p.q6DensityRelaxationTime = parse_double(value, key);\n        else if (key == "closedCapacityResponseEnable") p.closedCapacityResponseEnable = parse_bool(value, key);\n', '        else if (key == "q6DensityRelaxationBeta" || key == "densityRelaxationBeta") p.q6DensityRelaxationBeta = parse_double(value, key);\n        else if (key == "q6DensityRelaxationTime" || key == "densityRelaxationTime" || key == "densityRelaxationTau") p.q6DensityRelaxationTime = parse_double(value, key);\n        else if (key == "q6DensityRelaxationCompressionGateEnable" || key == "densityRelaxationCompressionGateEnable") p.q6DensityRelaxationCompressionGateEnable = parse_bool(value, key);\n        else if (key == "q6DensityRelaxationCompressionThresholdFill" || key == "densityRelaxationCompressionThresholdFill") p.q6DensityRelaxationCompressionThresholdFill = parse_double(value, key);\n        else if (key == "closedCapacityResponseEnable") p.closedCapacityResponseEnable = parse_bool(value, key);\n', 'params parser x7d-v2')
replace_once('src/params_io_base.cpp', '    if (!(q6DensityRelaxationEffectiveBeta0493x7d >= 0.0) ||\n        !(q6DensityRelaxationEffectiveBeta0493x7d <= 1.0) ||\n        !std::isfinite(q6DensityRelaxationEffectiveBeta0493x7d)) {\n        throw std::runtime_error(\n            "0493x7d effective density relaxation beta=dt/tau must be finite and lie in [0,1]");\n    }\n    if (q6DensityRelaxationEffectiveBeta0493x7d > 0.0 &&\n', '    if (!(q6DensityRelaxationEffectiveBeta0493x7d >= 0.0) ||\n        !(q6DensityRelaxationEffectiveBeta0493x7d <= 1.0) ||\n        !std::isfinite(q6DensityRelaxationEffectiveBeta0493x7d)) {\n        throw std::runtime_error(\n            "0493x7d effective density relaxation beta=dt/tau must be finite and lie in [0,1]");\n    }\n    if (!(p.q6DensityRelaxationCompressionThresholdFill >= 0.0) ||\n        !std::isfinite(p.q6DensityRelaxationCompressionThresholdFill)) {\n        throw std::runtime_error(\n            "0493x7d-v2 compression threshold fill must be finite and non-negative");\n    }\n    if (p.q6DensityRelaxationCompressionGateEnable &&\n        !(p.q6DensityRelaxationCompressionThresholdFill > 0.0)) {\n        throw std::runtime_error(\n            "0493x7d-v2 compression gate requires q6DensityRelaxationCompressionThresholdFill>0");\n    }\n    if (p.q6DensityRelaxationCompressionGateEnable &&\n        !(q6DensityRelaxationEffectiveBeta0493x7d > 0.0)) {\n        throw std::runtime_error(\n            "0493x7d-v2 compression gate requires active density relaxation");\n    }\n    if (q6DensityRelaxationEffectiveBeta0493x7d > 0.0 &&\n', 'params validation x7d-v2')
replace_once('src/cuda_q6_resident_0400.cu', '__device__ double q6_density_relaxation_target_divergence_0493x7c(\n    const double* rawFill,\n    const unsigned char* pressureMask,\n    int c,\n    int nx,\n    int ny,\n    int periodicX,\n    int periodicY,\n    double beta,\n    double dt,\n    int enabled) {\n    if (!enabled || rawFill == nullptr || pressureMask == nullptr ||\n        !(beta > 0.0) || !(dt > 0.0) || pressureMask[c] == 0u) {\n        return 0.0;\n    }\n\n    const int ix = c % nx;\n    const int iy = c / nx;\n    bool bulk = true;\n    if (periodicX || ix > 0) {\n        const int xw = periodicX ? wrap_cell_index_0400(ix - 1, nx) : ix - 1;\n        bulk = bulk && pressureMask[iy * nx + xw] != 0u;\n    }\n    if (periodicX || ix < nx - 1) {\n        const int xe = periodicX ? wrap_cell_index_0400(ix + 1, nx) : ix + 1;\n        bulk = bulk && pressureMask[iy * nx + xe] != 0u;\n    }\n    if (periodicY || iy > 0) {\n        const int ys = periodicY ? wrap_cell_index_0400(iy - 1, ny) : iy - 1;\n        bulk = bulk && pressureMask[ys * nx + ix] != 0u;\n    }\n    if (periodicY || iy < ny - 1) {\n        const int yn = periodicY ? wrap_cell_index_0400(iy + 1, ny) : iy + 1;\n        bulk = bulk && pressureMask[yn * nx + ix] != 0u;\n    }\n    if (!bulk) return 0.0;\n\n    const double defect = rawFill[c] - 1.0;\n    return isfinite(defect) ? beta * defect / dt : 0.0;\n}\n', '__device__ double q6_density_relaxation_target_divergence_0493x7c(\n    const double* rawFill,\n    const unsigned char* pressureMask,\n    int c,\n    int nx,\n    int ny,\n    int periodicX,\n    int periodicY,\n    double beta,\n    double dt,\n    double compressionThresholdFill0493x7dv2,\n    int compressionGateEnable0493x7dv2,\n    int enabled) {\n    if (!enabled || rawFill == nullptr || pressureMask == nullptr ||\n        !(beta > 0.0) || !(dt > 0.0) || pressureMask[c] == 0u) {\n        return 0.0;\n    }\n\n    const int ix = c % nx;\n    const int iy = c / nx;\n    bool bulk = true;\n    if (periodicX || ix > 0) {\n        const int xw = periodicX ? wrap_cell_index_0400(ix - 1, nx) : ix - 1;\n        bulk = bulk && pressureMask[iy * nx + xw] != 0u;\n    }\n    if (periodicX || ix < nx - 1) {\n        const int xe = periodicX ? wrap_cell_index_0400(ix + 1, nx) : ix + 1;\n        bulk = bulk && pressureMask[iy * nx + xe] != 0u;\n    }\n    if (periodicY || iy > 0) {\n        const int ys = periodicY ? wrap_cell_index_0400(iy - 1, ny) : iy - 1;\n        bulk = bulk && pressureMask[ys * nx + ix] != 0u;\n    }\n    if (periodicY || iy < ny - 1) {\n        const int yn = periodicY ? wrap_cell_index_0400(iy + 1, ny) : iy + 1;\n        bulk = bulk && pressureMask[yn * nx + ix] != 0u;\n    }\n    if (!bulk) return 0.0;\n\n    const double defect = rawFill[c] - 1.0;\n    if (!isfinite(defect)) return 0.0;\n\n    // 0493x7d-v2: classify positive coherent compression before applying the\n    // historical x7d target. Once classified, keep the full defect: this is a\n    // gate, not a dead-band subtraction.\n    if (compressionGateEnable0493x7dv2) {\n        if (!(compressionThresholdFill0493x7dv2 > 0.0) ||\n            defect < compressionThresholdFill0493x7dv2) {\n            return 0.0;\n        }\n\n        bool coherent0493x7dv2 = false;\n        if (periodicX || ix > 0) {\n            const int xw = periodicX ? wrap_cell_index_0400(ix - 1, nx) : ix - 1;\n            const double nd = rawFill[iy * nx + xw] - 1.0;\n            coherent0493x7dv2 =\n                isfinite(nd) && nd >= compressionThresholdFill0493x7dv2;\n        }\n        if (!coherent0493x7dv2 && (periodicX || ix < nx - 1)) {\n            const int xe = periodicX ? wrap_cell_index_0400(ix + 1, nx) : ix + 1;\n            const double nd = rawFill[iy * nx + xe] - 1.0;\n            coherent0493x7dv2 =\n                isfinite(nd) && nd >= compressionThresholdFill0493x7dv2;\n        }\n        if (!coherent0493x7dv2 && (periodicY || iy > 0)) {\n            const int ys = periodicY ? wrap_cell_index_0400(iy - 1, ny) : iy - 1;\n            const double nd = rawFill[ys * nx + ix] - 1.0;\n            coherent0493x7dv2 =\n                isfinite(nd) && nd >= compressionThresholdFill0493x7dv2;\n        }\n        if (!coherent0493x7dv2 && (periodicY || iy < ny - 1)) {\n            const int yn = periodicY ? wrap_cell_index_0400(iy + 1, ny) : iy + 1;\n            const double nd = rawFill[yn * nx + ix] - 1.0;\n            coherent0493x7dv2 =\n                isfinite(nd) && nd >= compressionThresholdFill0493x7dv2;\n        }\n        if (!coherent0493x7dv2) return 0.0;\n    }\n\n    return beta * defect / dt;\n}\n', 'CUDA x7d helper')
replace_once('src/cuda_q6_resident_0400.cu', '    const double* densityRelaxationRawFill0493x7c,\n    double densityRelaxationBeta0493x7c,\n    double densityRelaxationDt0493x7c,\n    int densityRelaxationEnable0493x7c,\n    int fullDomain) {\n', '    const double* densityRelaxationRawFill0493x7c,\n    double densityRelaxationBeta0493x7c,\n    double densityRelaxationDt0493x7c,\n    double densityRelaxationCompressionThresholdFill0493x7dv2,\n    int densityRelaxationCompressionGateEnable0493x7dv2,\n    int densityRelaxationEnable0493x7c,\n    int fullDomain) {\n', 'RHS kernel signature')
replace_once('src/cuda_q6_resident_0400.cu', '    const double* densityRelaxationRawFill0493x7c,\n    double densityRelaxationBeta0493x7c,\n    double densityRelaxationDt0493x7c,\n    int densityRelaxationEnable0493x7c,\n    double* partialTargetSq0493x7c,\n    int fullDomain) {\n', '    const double* densityRelaxationRawFill0493x7c,\n    double densityRelaxationBeta0493x7c,\n    double densityRelaxationDt0493x7c,\n    double densityRelaxationCompressionThresholdFill0493x7dv2,\n    int densityRelaxationCompressionGateEnable0493x7dv2,\n    int densityRelaxationEnable0493x7c,\n    double* partialTargetSq0493x7c,\n    int fullDomain) {\n', 'stats kernel signature')
replace_once('scripts/src_mpcd_run_common_0434.sh', '  local tau="${Q6_GF_DENSITY_RELAXATION_TIME:-0.25}"\n  local min_fill="${Q6_GF_MIN_FILL_FRACTION:-0.10}"\n  cat <<PARAMS\n', '  local tau="${Q6_GF_DENSITY_RELAXATION_TIME:-0.25}"\n  local min_fill="${Q6_GF_MIN_FILL_FRACTION:-0.10}"\n  local compression_gate=false\n  if suite_truthy_0434 "${Q6_GF_DENSITY_COMPRESSION_GATE_ENABLE:-0}"; then\n    compression_gate=true\n  fi\n  local compression_threshold_particles="${Q6_GF_DENSITY_COMPRESSION_THRESHOLD_PARTICLES:-3.0}"\n  local compression_threshold_fill\n  compression_threshold_fill="$(awk -v n="$compression_threshold_particles" -v g="$GAMMA" \'BEGIN{\n    if (!(n>0) || !(g>0)) exit 2;\n    printf "%.17g", n/g\n  }\')" || {\n    echo "[0493x7d-v2] ERROR compression threshold requires positive particle threshold and GAMMA" >&2\n    return 2\n  }\n  cat <<PARAMS\n', 'common helper locals')
replace_once('scripts/src_mpcd_run_common_0434.sh', 'q6DensityRelaxationBeta = 0.0\nq6DensityRelaxationTime = ${tau}\nkeepMeanFlowEnable = false\n', 'q6DensityRelaxationBeta = 0.0\nq6DensityRelaxationTime = ${tau}\nq6DensityRelaxationCompressionGateEnable = ${compression_gate}\nq6DensityRelaxationCompressionThresholdFill = ${compression_threshold_fill}\nkeepMeanFlowEnable = false\n', 'common helper params')
replace_once('scripts/src_mpcd_run_common_0434.sh', '    grep -Eq \'^[[:space:]]*q6DensityRelaxationTime[[:space:]]*=[[:space:]]*[0-9.eE+-]+\' "$params" || {\n      echo "[0493x7h-run-ok] ERROR src-q6-g-f density-restoration time missing" >&2; return 2; }\n    if grep -Eq \'^[[:space:]]*darcyBrinkmanEnable[[:space:]]*=[[:space:]]*true([[:space:]]|$)\' "$params"; then\n', '    grep -Eq \'^[[:space:]]*q6DensityRelaxationTime[[:space:]]*=[[:space:]]*[0-9.eE+-]+\' "$params" || {\n      echo "[0493x7h-run-ok] ERROR src-q6-g-f density-restoration time missing" >&2; return 2; }\n    if suite_truthy_0434 "${Q6_GF_DENSITY_COMPRESSION_GATE_ENABLE:-0}"; then\n      grep -Eq \'^[[:space:]]*q6DensityRelaxationCompressionGateEnable[[:space:]]*=[[:space:]]*true([[:space:]]|$)\' "$params" || {\n        echo "[0493x7d-v2-run-ok] ERROR compression gate enable missing" >&2; return 2; }\n      grep -Eq \'^[[:space:]]*q6DensityRelaxationCompressionThresholdFill[[:space:]]*=[[:space:]]*[0-9.eE+-]+\' "$params" || {\n        echo "[0493x7d-v2-run-ok] ERROR compression threshold fill missing" >&2; return 2; }\n    fi\n    if grep -Eq \'^[[:space:]]*darcyBrinkmanEnable[[:space:]]*=[[:space:]]*true([[:space:]]|$)\' "$params"; then\n', 'common preflight')

cu = ROOT / 'src/cuda_q6_resident_0400.cu'
text = cu.read_text()
old_call = '                densityRelaxationRawFill0493x7c, mask, c, nx, ny,\n                periodicX, periodicY, densityRelaxationBeta0493x7c,\n                densityRelaxationDt0493x7c, 1);\n'
new_call = '                densityRelaxationRawFill0493x7c, mask, c, nx, ny,\n                periodicX, periodicY, densityRelaxationBeta0493x7c,\n                densityRelaxationDt0493x7c,\n                densityRelaxationCompressionThresholdFill0493x7dv2,\n                densityRelaxationCompressionGateEnable0493x7dv2, 1);\n'
n = text.count(old_call)
if n != 2:
    raise SystemExit(f"[0493x7d-v2] ERROR expected 2 device helper calls, got {n}")
text = text.replace(old_call, new_call)
old_launch = '            densityRelaxationRequested0493x7c ? ws.phaseFillRaw0493x6c.data() : nullptr,\n            densityRelaxationBeta0493x7d, params.dt,\n            densityRelaxationRequested0493x7c ? 1 : 0,\n'
new_launch = '            densityRelaxationRequested0493x7c ? ws.phaseFillRaw0493x6c.data() : nullptr,\n            densityRelaxationBeta0493x7d, params.dt,\n            params.q6DensityRelaxationCompressionThresholdFill,\n            params.q6DensityRelaxationCompressionGateEnable ? 1 : 0,\n            densityRelaxationRequested0493x7c ? 1 : 0,\n'
n = text.count(old_launch)
if n != 2:
    raise SystemExit(f"[0493x7d-v2] ERROR expected 2 host launch argument sites, got {n}")
text = text.replace(old_launch, new_launch)
cu.write_text(text)

checks = {
  'include/simulation_params.h': ['q6DensityRelaxationCompressionGateEnable','q6DensityRelaxationCompressionThresholdFill'],
  'src/params_io_base.cpp': ['0493x7d-v2 compression gate requires','densityRelaxationCompressionThresholdFill'],
  'src/cuda_q6_resident_0400.cu': ['compressionGateEnable0493x7dv2','coherent0493x7dv2','params.q6DensityRelaxationCompressionThresholdFill'],
  'scripts/src_mpcd_run_common_0434.sh': ['Q6_GF_DENSITY_COMPRESSION_GATE_ENABLE','Q6_GF_DENSITY_COMPRESSION_THRESHOLD_PARTICLES','q6DensityRelaxationCompressionThresholdFill'],
}
for rel, needles in checks.items():
    text = (ROOT/rel).read_text()
    for needle in needles:
        if needle not in text:
            raise SystemExit(f"[0493x7d-v2] ERROR post-check missing {needle!r} in {rel}")
dc = run('git','diff','--check')
if dc.returncode != 0:
    raise SystemExit("[0493x7d-v2] ERROR git diff --check failed:\n" + dc.stdout)

print(f"[0493x7d-v2] branch={branch}")
print(f"[0493x7d-v2] base={head[:12]}")
print("[0493x7d-v2] historical raw x7d remains default (gate=false)")
print("[0493x7d-v2] gate = positive dN>=thetaN + >=1 N/S/E/W neighbour")
print("[0493x7d-v2] default thetaN=3 particles; runner writes thresholdFill=thetaN/GAMMA")
print("[0493x7d-v2] accepted cells receive full historical x7d target")
print("[0493x7d-v2] no extra CUDA kernel or persistent buffer added")
print("[0493x7d-v2] git diff --check PASS")
print("[0493x7d-v2] done")
